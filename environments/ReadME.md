# Examples of generated environments

* _example 1_ : a complete example with the generated Prolog code (.pl), JSON description (.json) and PyGame rendering of the virtual house (.png)
* _environment*.json_ : other examples
